{
  "name": "@paramet/tolgeet",
  "version": "1.0.4",
  "description": "Gets and transforms language files from tolgee.",
  "main": "dist/index.js",
  "exports": {
    ".": "./dist/index.js",
    "./package": "./package.json"
  },
  "files": [
    "/dist/",
    "!/dist/test",
    "/LICENSE"
  ],
  "bin": "dist/index.js",
  "scripts": {
    "clean": "rimraf dist",
    "build": "tsc",
    "rebuild": "npm run clean && npm run build",
    "prepare": "npm run rebuild",
    "start": "npm run build && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "tolgee",
    "i18n",
    "language",
    "resx",
    "locale",
    "localization",
    "json"
  ],
  "author": "Paramet Limited",
  "license": "MIT",
  "dependencies": {
    "@types/flat": "^5.0.2",
    "@types/resx": "^2.0.1",
    "command-line-args": "^5.2.1",
    "flat": "^5.0.2",
    "jszip": "^3.10.0",
    "resx": "^2.0.3",
    "rimraf": "^3.0.2"
  },
  "devDependencies": {
    "@types/command-line-args": "^5.2.0",
    "@types/node": "^18.0.3",
    "typescript": "^4.7.4"
  }
}
